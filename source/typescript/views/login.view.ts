import { View } from '../_core/_app.core';

export default class LoginView implements View {
    private $appContent: HTMLElement = document.getElementById("content")!;

    public async render(__ClientData__: any) {

    }
}